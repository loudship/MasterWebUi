services:
  calendar-mcp:
    environment:
      GOOGLE_APPLICATION_CREDENTIALS: /run/secrets/google-calendar-adc.json
    volumes:
      - ${GOOGLE_APPLICATION_CREDENTIALS_HOST_PATH:?Set GOOGLE_APPLICATION_CREDENTIALS_HOST_PATH to an external ADC JSON file}:/run/secrets/google-calendar-adc.json:ro
